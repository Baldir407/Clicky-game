import React from "react";
import "./Header.css";

const Header = props => (
  <header className="header">
    <h1>Klicky Kombat!</h1>
    <h2>
      Klick on an image to earn points! Click on the same image twice and you will no failure!
    </h2>
  </header>
);

export default Header;
